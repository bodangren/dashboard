Tasks 1.3 & 1.4: main.go skeleton + build verification
Summary: Created root-level main.go (moved from cmd/dashboard to support go:embed path constraints). Wires HTTP server on :8080, embeds static/, registers /api/projects and /api/diff routes via api.RegisterRoutes. Stub handlers return empty JSON. go build ./... passes.
Files: main.go, internal/api/handlers.go
Deviation: main.go placed at root instead of cmd/dashboard/ because go:embed cannot use ../ paths.
